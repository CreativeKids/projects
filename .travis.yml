language: python
python: 2.7
cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/lektor/builds
install: ./travis-install.sh
script: cd school-website && lektor build
deploy:
  provider: script
  script: "lektor deploy ghpages"
